# 审核证据与审计记录

这里仅保留随 Vault 提交的人工可读审核摘要。机器可验证的 ReviewPacket、DecisionReceipt 与 ConfirmationReceipt 位于 Wiki 模块外层 `.review_queue/`，审计事件位于模块外层 `audit_trail.jsonl`；这些 JSON/JSONL 不进入 Obsidian Vault。

[[wiki-sdtm-spec-playbook-approval|SDTM Spec 基线批准记录]]
