# Derived parser outputs

后续解析产物放在这里。

预期子目录：

- `crf/`：从 CRF 来源解析出的字段结构 JSON。
- `edc/`：从 EDC data dictionary 解析出的字段结构 JSON。
- `study-context/`：从 protocol/SAP/current study notes 解析出的 Study rule 候选 JSON。

当前脚手架阶段不生成这些 JSON。
