# Work area

`work/` is the machine-derived intermediate area, not a source area.

- `derived/` stores parser or LLM outputs such as structured JSON, source hash manifests, and parser validation reports.
- `mapping/` stores MappingSpec candidates and mapping validation reports.

These files cannot drive programming or canonical promotion until the matching Review gate is approved.
